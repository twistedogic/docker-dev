# Contents of /etc/logstash.conf
input {
 file {
 type => "nginx_access"
 path => ["/var/log/nginx/**"]
 exclude => ["*.gz", "error.*"]
 discover_interval => 10
 }
}
 
filter {
 grok {
 type => nginx_access
 pattern => "%{COMBINEDAPACHELOG}"
 }
}
 
output {
 redis { host => "192.168.99.102" data_type => "list" key => "logstash" }
}